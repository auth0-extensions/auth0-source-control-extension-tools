import constants from './constants';
import deploy from './deploy';
import { keywordReplace, loadFile } from './utils';

export default {
  constants,
  deploy,
  keywordReplace,
  loadFile
};


export {
  constants,
  deploy,
  keywordReplace,
  loadFile
};
